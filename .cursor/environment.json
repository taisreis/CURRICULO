{
  "name": "Currículo Taís Reis",
  "ports": [8000],
  "start": "(curl -sf -o /dev/null http://localhost:8000/ && echo 'static-server already running on :8000') || (nohup python3 -m http.server 8000 --directory /workspace > /tmp/static-server.log 2>&1 & echo 'static-server started on :8000')"
}
